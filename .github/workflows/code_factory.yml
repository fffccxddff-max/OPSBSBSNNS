name: 24-7 Pure Local AI Coder Bot (1.5B Edition)

on:
  schedule:
    - cron: '0 */5 * * *' # تمدید خودکار هر ۵ ساعت
  workflow_dispatch: # استارت دستی

jobs:
  run-local-bot:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      # سیستم کَش اختصاصی برای مدل 1.5B
      - name: Cache Hugging Face Models
        uses: actions/cache@v4
        with:
          path: ~/.cache/huggingface
          key: ${{ runner.os }}-hf-qwen-1.5b
          restore-keys: |
            ${{ runner.os }}-hf-

      - name: Install Dependencies
        run: |
          pip install torch transformers accelerate requests

      - name: Run Local Agent Daemon
        env:
          TELEGRAM_TOKEN: "8940324884:AAEZPFjiZlUKd1iuNT4QPBZ6vr0f2ee593c"
        run: python bot_daemon.py
